import { } from '../{{MODULE_NAME_LOWER}}.enum';
import { IsEmail, IsIn, IsNumber, IsOptional, Length } from 'class-validator';
import { ApiProperty } from '@nestjs/swagger';

export class Update{{MODULE_NAME}}Dto {
{{MAIN_WORKSPACE}}    
}